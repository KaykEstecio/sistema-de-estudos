# Ambiente do Docker Compose; configuração do FastAPI fica em backend/.env.
POSTGRES_DB=codetrack
POSTGRES_USER=codetrack
# Preencha apenas no .env local com uma senha própria.
POSTGRES_PASSWORD=
POSTGRES_PORT=5432
